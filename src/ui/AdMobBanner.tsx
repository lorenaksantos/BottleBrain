import * as React from 'react';
import { View } from 'react-native';

/**
 * AdMobBanner — stub
 *
 * Renders a harmless empty space where the ad will eventually go.
 * To activate real ads:
 *   1. npm install react-native-google-mobile-ads
 *   2. Add the plugin back to app.config.js
 *   3. Do an EAS build
 *   4. Replace this file with the full implementation
 */
export function AdMobBanner() {
  return <View style={{ height: 50 }} />;
}
